import React, { ReactElement } from "react";

interface Props {}

function ParentPositivePost(): ReactElement {
  return (
      <table>

      </table>
    
  );
}

export default ParentPositivePost;

